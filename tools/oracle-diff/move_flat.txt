# 移動範囲ハーネス検証コーパス (FLAT)。
# 7x7 平地マップ、中央 (3,3) に単一ユニットを置き到達マスを diff する。
# 全セル move_cost 1 のため、両エンジンは同一のマンハッタン菱形 (速度=移動半径) を
# 同一コストで返すはず。EXPECT: C# と Rust の到達集合 + コストが完全一致。
#
# 注: 1 マップ 1 ユニットにする。C# StandBy は配置先が占有/進入不可だと近傍へずらすため、
# 複数ユニットを同一セルへ置くと start がずれて diff がノイズ化する (検証済み)。
@map 7 7
# マジンガーＺ: 陸 (ground)、speed 5、adaption -ACB。
@unit マジンガーＺ 0 味方 人工知能 10 3 3
@move マジンガーＺ
